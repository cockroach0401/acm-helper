name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags

jobs:
  build-backend:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build EXE
        run: |
          cd backend
          pyinstaller build.spec
          Copy-Item -Path "icon.ico" -Destination "dist/icon.ico"
      
      - name: Package Backend
        run: |
          cd backend/dist
          Compress-Archive -Path ACMHelper.exe,icon.ico -DestinationPath ACMHelper-Windows.zip
      
      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ACMHelper-Windows
          path: backend/dist/ACMHelper-Windows.zip

  build-extension:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Package Extension
        run: |
          cd browser-extension
          zip -r ../acm-helper-extension.zip . -x "*.git*" -x "*.md"
      
      - name: Upload Extension Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ACMHelper-Extension
          path: acm-helper-extension.zip

  release:
    needs: [build-backend, build-extension]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ACMHelper-Windows/ACMHelper-Windows.zip
            ACMHelper-Extension/acm-helper-extension.zip
          generate_release_notes: true
